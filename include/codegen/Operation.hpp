#pragma once

namespace phantom {
  namespace codegen {
    namespace op {
      // void add();
      // void sub();
      // void mul();
      // void div();
      // void asgn();
      // void neg();
    }
  }
}
